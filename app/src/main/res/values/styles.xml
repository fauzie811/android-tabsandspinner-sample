<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="AppTheme" parent="AppTheme.Base">
        <item name="colorPrimary">@color/theme_primary</item>
        <item name="colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="colorAccent">@color/theme_accent_1</item>

        <item name="actionBarInsetStart">@dimen/keyline_2</item>

        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.NoIcon" parent="AppTheme">
        <item name="actionBarInsetStart">@dimen/keyline_1</item>
    </style>

    <style name="AppTheme.WithSpinner" parent="AppTheme">
        <item name="actionBarInsetStart">@dimen/keyline_2_minus_16dp</item>
    </style>

    <!-- For Lollipop only -->
    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:textColorPrimary">#fff</item>
        <item name="colorControlNormal">#fff</item>
        <item name="colorControlHighlight">#3fff</item>
    </style>

    <style name="Widget.HeaderBar.Spinner.Base" parent="Widget.AppCompat.Spinner.DropDown.ActionBar">
        <item name="android:theme">@style/ActionBarThemeOverlay</item>

        <item name="android:background">?selectableItemBackgroundBorderless</item>
        <item name="android:dropDownVerticalOffset">-52dp</item>
    </style>
    <style name="Widget.HeaderBar.Spinner" parent="Widget.HeaderBar.Spinner.Base">
    </style>

    <style name="HeaderBar">
        <item name="android:background">?colorPrimary</item>
        <item name="android:elevation">4dp</item>
    </style>

    <style name="TabIndicator">
        <item name="android:theme">@style/ActionBarThemeOverlay</item>
    </style>

    <style name="SpinnerItemText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
